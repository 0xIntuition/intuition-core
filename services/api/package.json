{
	"name": "@0xintuition/api",
	"private": true,
	"type": "module",
	"license": "MIT",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"up": "bun src/index.ts",
		"start": "bun src/index.ts",
		"build": "tsc",
		"clean": "git clean -xdf .cache .turbo dist node_modules",
		"typecheck": "tsc --noEmit --emitDeclarationOnly false",
		"test": "bun test tests",
		"ci": "biome check",
		"lint": "biome lint",
		"format": "biome format",
		"check": "biome check",
		"check:write": "biome check --write",
		"keys:create": "bun scripts/keys.ts create",
		"keys:list": "bun scripts/keys.ts list",
		"keys:revoke": "bun scripts/keys.ts revoke"
	},
	"dependencies": {
		"@0xintuition/database-kg": "workspace:*",
		"drizzle-orm": "0.45.1",
		"hono": "^4.11.6"
	},
	"devDependencies": {
		"@0xintuition/tsconfig": "workspace:*",
		"@types/bun": "^1.3.3",
		"typescript": "catalog:"
	}
}
