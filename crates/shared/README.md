# `shared`

Shared runtime types, configuration helpers, and error models for Core Rust
services.

This crate is source-only for now. It is intentionally not part of the first
crates.io publishing wave because its types still reflect internal service
contracts rather than a stable external API.
